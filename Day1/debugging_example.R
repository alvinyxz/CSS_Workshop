# Debugging Example in R
# This script contains some common mistakes
# Use ChatGPT to help identify and fix them
# Remember, you can attach various information to ChatGPT: The file (Day1.csv), your code, the error message, etc
# You can even send the entire script to ChatGPT to fix

mydata <- read.csv("Day1.csv")

mean_fav <- mean(mydata$favorits)

summary(retweet)

cor(mydata$Party, mydata$affiliation)

subset(mydata, favorites > "100")

head(MyData)

plot(mydata$favorites mydata$retweets)
